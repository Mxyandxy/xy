# 生产环境后端地址（Vite 构建时自动注入）
# 注意：必须带 /api 后缀，前端请求不带 /api 前缀，靠这里补上
#
# 不要在这里硬编码真实地址。请在部署平台（Cloudflare Pages）
# 的 Environment variables 里设置 VITE_API_URL，例如：
#   VITE_API_URL = https://campus-forum-api.onrender.com/api
#
# Vite 规则：运行时已存在的环境变量优先级最高，不会被 .env 文件覆盖，
# 所以平台侧设置的值会生效，这里只需留占位符。
VITE_API_URL=https://REPLACE-ME.onrender.com/api
